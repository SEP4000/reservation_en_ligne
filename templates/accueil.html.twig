{% extends 'base.html.twig' %}

{% block title %}Accueil{% endblock %}

{% block body %}
     <div class="container text-center mt-5">
        <h1 class="mb-4">Bienvenue sur notre système de réservation</h1>

        <div class="d-flex justify-content-center">
            <a href="{{ path('app_reservation') }}" class="btn btn-primary mx-2">Prendre une réservation</a>
            <a href="{{ path('app_reservation_list') }}" class="btn btn-secondary mx-2">Voir les Réservations</a>
        </div>
    </div>
{% endblock %}
